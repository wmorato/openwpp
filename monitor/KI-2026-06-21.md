# KI: WhatsApp Reconnect & Fixes — 2026-06-21

## Problema Raiz
1. **ms-prospeccao** bloqueado: `EVO_API_URL` e `EVO_API_KEY` ausentes em `/var/www/secrets/ms-prospeccao.env`. Qualquer chamada a `/conexao/conectar` retornava 500.
2. **ms-openwpp** stuck "Aguardando" (>24h): sessão WhatsApp (whatsapp-web.js) corrompida. Chrome rodava mas cliente nunca emitia 'qr' nem 'ready'. Nenhum log de erro após restart em Jun 20 15:04.
3. **Health check ruidoso**: alertava "WhatsApp desconectado" a cada hora mesmo quando estado era normal (QR aguardando scan).

## Correções
1. **ms-prospeccao.env** — adicionado `EVO_API_URL=http://localhost:5030` e `EVO_API_KEY=d44bacf08951f12db28f5e083a7e613c7636e4f6769441ce`
2. **evolutionService.js** — removido (arquivo morto, 0 bytes, sem imports)
3. **ms-openwpp** — sessão stale em `/var/www/.wwebjs_auth_openwpp/session` movida para backup, service restartado via systemd. QR Code gerado imediatamente.
4. **HttpHandler.mjs** — adicionado `GET /api/qrcode` (JSON ou `?format=image`) e campo `hasQrCode` no `/api/health`
5. **check_health.sh** — agora diferencia "Aguardando com QR" (aguardando scan) de "Aguardando sem QR" (problema real)

## Estado Atual
- Evolution API MS_Morato: **open** ✅
- ms-prospeccao env vars: **configuradas** ✅
- ms-openwpp: QR gerado, **aguardando scan** 📱

## Ação Pendente
- Escanear QR Code em `chat.moratosolucoes.com.br` ou `GET /api/qrcode?format=image`
